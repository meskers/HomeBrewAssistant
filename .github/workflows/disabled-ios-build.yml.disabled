# This workflow is temporarily disabled to stop failure emails
# Rename to ios-build.yml to re-enable

name: iOS Build and Test (DISABLED)

on:
  workflow_dispatch:  # Only manual trigger

jobs:
  build:
    name: Build Check
    runs-on: macos-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Success
      run: |
        echo "🎉 Workflow disabled to prevent email spam"
        echo "Re-enable by renaming this file to ios-build.yml" 